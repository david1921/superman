<%= {
  :methods => {
    :update_password => publisher_consumer_password_reset_url(@publisher.to_param, @consumer.perishable_token, :host => AppConfig.api_host, :format => "json")
  }
}.to_json.html_safe -%>