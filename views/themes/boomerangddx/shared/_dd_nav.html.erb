<ul id="dd_nav">
	<%= render :partial => with_theme('shared/market_menu') if @publisher.enable_market_menu %>
	<li><a href="<%= public_deal_of_day_path(@publisher.label) %>" class="first <%= "#{controller_name}##{action_name}" == "publishers#deal_of_the_day" ? 'current_page' : nil %>">Today's Deal</a></li>
	<% if @publisher.allow_all_deals_page %>
	<li>
	<a href="<%= daily_deals_public_index_path(@publisher.label) %>?per_page=6" class="<%= "#{controller_name}##{action_name}" == "daily_deals#public_index" ? 'current_page' : nil %>">All Deals</a>
	</li>
	<% end %>
	<li><a href="<%= how_it_works_publisher_daily_deals_path(@publisher) %>" class="<%= "#{controller_name}##{action_name}" == "daily_deals#how_it_works" ? 'current_page' : nil %>">How it Works</a></li>
	<li><a href="<%= faqs_publisher_daily_deals_path(@publisher) %>" class="<%= "#{controller_name}##{action_name}" == "daily_deals#faqs" ? 'current_page' : nil %>">FAQ</a></li>
	<li><a href="<%= contact_publisher_daily_deals_path(@publisher) %>" class="<%= "#{controller_name}##{action_name}" == "daily_deals#contact" ? 'current_page' : nil %>">Contact Us</a></li>
</ul><!-- /dd_nav -->